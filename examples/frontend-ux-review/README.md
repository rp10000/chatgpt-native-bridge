# Frontend UX review

```bash
cgn ask \
  --task "Review the first-run onboarding screen" \
  --type ux-review,naming-copy \
  --include-screenshots "screenshots/*.png" \
  --include-files "src/components/*.tsx"
```

Upload `ask.md`, `context.md`, and the `screenshots/` folder to ChatGPT.
